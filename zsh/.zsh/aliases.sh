# Check my IP
alias myip="curl http://ipecho.net/plain; echo"

# Detailed list of directory
alias l="ls -lAFh"

# Symlink from `stow` command to `dot` command
alias dot="stow"

# Change directory to dotfiles
alias dotcd="cd ~/.dotfiles"

# Soucing zshrc files
alias reset="command reset && source ~/.zshrc"